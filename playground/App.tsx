export default function App() {
  return (
    <div className="bg-page min-h-screen px-10 py-12">
      <div className="mx-auto max-w-[720px]">
        <div className="text-ink-3 text-[10.5px] font-medium tracking-[0.14em] uppercase">
          @rye-api/rewards-ui
        </div>
        <h1 className="text-ink-1 mt-2 text-[28px] font-semibold -tracking-[0.02em]">Playground</h1>
        <p className="text-ink-2 mt-4 text-[14px] leading-relaxed">
          Foundation is wired up. Tailwind tokens, vitest, ESLint, and the shadcn registry build are
          all running. Component stories land per PR as each component ships.
        </p>
      </div>
    </div>
  );
}
